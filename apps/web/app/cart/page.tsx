import { products } from "@/lib/data";
import { formatCurrency } from "@/lib/format";
export default function CartPage(){ const cart=products.slice(0,3); const total=cart.reduce((s,p)=>s+p.price,0); return <main className="premium-container py-10"><h1 className="mb-8 text-4xl font-black text-marketDark">Your cart</h1><div className="grid gap-8 lg:grid-cols-[1fr_380px]"><div className="grid gap-4">{cart.map(p=><div key={p.id} className="flex gap-4 rounded-[2rem] bg-white p-4 shadow-soft"><img src={p.image} className="h-28 w-28 rounded-2xl object-cover" alt={p.name}/><div className="flex-1"><p className="font-black text-marketDark">{p.name}</p><p className="text-sm text-marketDark/50">{p.vendor}</p><p className="mt-3 font-black text-marketGreen">{formatCurrency(p.price)}</p></div></div>)}</div><aside className="h-fit rounded-[2rem] bg-marketDark p-6 text-white shadow-premium"><h2 className="text-2xl font-black">Order summary</h2><div className="mt-6 flex justify-between"><span>Subtotal</span><span>{formatCurrency(total)}</span></div><div className="mt-3 flex justify-between"><span>Delivery</span><span>{formatCurrency(5000)}</span></div><div className="mt-6 border-t border-white/10 pt-6 flex justify-between text-xl font-black"><span>Total</span><span className="text-marketCream">{formatCurrency(total+5000)}</span></div><a href="/checkout" className="mt-6 block rounded-full bg-marketCream px-6 py-4 text-center font-black text-marketDark">Proceed to checkout</a></aside></div></main> }
