import { Button } from '@/components/ui/button';
import { Card } from '@/components/ui/card';
import { money } from '@/lib/utils';
async function getProduct(slug:string){ try{ const res=await fetch(`${process.env.NEXT_PUBLIC_API_URL}/products/${slug}`, { cache:'no-store' }); const json=await res.json(); return json.data; }catch{return null} }
export default async function ProductDetail({params}:{params:{slug:string}}){ const p=await getProduct(params.slug); if(!p) return <main className="p-10">Product not found</main>; return <main className="mx-auto grid max-w-7xl gap-8 px-4 py-10 md:grid-cols-2"><div className="h-96 rounded-2xl bg-muted"/><section><p className="text-sm font-semibold text-primary">{p.vendor?.storeName}</p><h1 className="mt-2 text-4xl font-black">{p.name}</h1><p className="mt-4 text-3xl font-black">{money(p.price)}</p><p className="mt-4 text-muted-foreground">{p.description}</p><div className="mt-6 flex gap-3"><Button>Add to Cart</Button><Button variant="outline">Wishlist</Button></div><Card className="mt-8"><h3 className="font-bold">AI Review Summary</h3><p className="mt-2 text-sm text-muted-foreground">Connect OpenAI to summarize customer reviews automatically.</p></Card></section></main> }
